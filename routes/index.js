const express = require('express');
const router  = express.Router();


router.get('/', (req, res, next) => {
  .then(() =>  res.status.json(200))
  
 ;
});

router.get('/teléfonos', (req,res, next) => {

})

module.exports = router;
